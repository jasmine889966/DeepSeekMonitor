# 安全策略

## 敏感数据

不要公开 DeepSeek token、cookie、HAR 文件、本地日志、`.env` 文件，或 `~/Library/Application Support/DeepSeekMonitor/credentials.json`。

如果 token 或 cookie 不小心被公开了，请立即在 DeepSeek 侧撤销会话，并轮换所有受影响的凭据。

## 报告方式

请将安全问题私下反馈给项目维护者。不要公开发布包含利用细节、token、cookie、账户 ID 或 HAR 附件的 issue。
